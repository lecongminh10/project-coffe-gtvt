@model int

<a class="cart-nav-pill" asp-controller="Cart" asp-action="Index" id="cartNavBtn" title="Xem giỏ hàng">
    <i class="fas fa-shopping-bag text-dark me-1"></i>
    <span>Giỏ Hàng</span>
    <span class="cart-pill-counter ms-1" id="cartBadgeCount">@Model</span>
</a>
